# Gradle
.gradle/
build/
**/build/
*.class
local.properties
*.iml

# Android
*.apk
*.aab
*.dex
*.ap_

# IDE
.idea/
*.iws
*.ipr
nbproject/
.DS_Store
.kotlin/

# Keys & secrets
*.keystore
*.jks
google-services.json
local.defaults.properties

# Offline data / large assets
*.pbf
*.mbtiles
*.db
*.sqlite
tools/data/

# Free-stack map-tile build tooling: planetiler.jar, raw Geofabrik extracts, and generated
# intermediate tile packages are large, regeneratable build artifacts, not source - see
# ZERO_COST_ARCHITECTURE.md for how to regenerate them. Only the final package actually bundled
# into the app (app/src/main/assets/*.mbtiles) is tracked.
tools/planetiler/

# Relay server dependencies
server/node_modules/
