<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="androidx.car.app.ACCESS_SURFACE" />
    <uses-permission android:name="androidx.car.app.NAVIGATION_TEMPLATES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_roadpulse"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoadPulse">

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="3" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        <meta-data
            android:name="com.roadpulse.auto.TANKERKOENIG_API_KEY"
            android:value="${TANKERKOENIG_API_KEY}" />
        <meta-data
            android:name="com.roadpulse.auto.OPEN_CHARGE_MAP_API_KEY"
            android:value="${OPEN_CHARGE_MAP_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".NavigationActivity"
            android:exported="false" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- Free-stack migration proof of concept, see ZERO_COST_ARCHITECTURE.md. Not reachable
             from any app screen. Non-exported: on this retail device, launching it via
             `adb shell am start` needs a temporary exported="true" edit (revert after use) since
             shell can't start a non-exported activity here - do not leave it exported. -->
        <activity
            android:name=".engine.MapLibrePocActivity"
            android:exported="false" />

        <!-- Free-stack migration proof of concept, see ZERO_COST_ARCHITECTURE.md. Not reachable
             from any app screen. Non-exported: use a temporary exported="true" edit to launch via
             `adb shell am start`, then revert - do not leave it exported. -->
        <activity
            android:name=".engine.GraphHopperPocActivity"
            android:exported="false" />

        <service
            android:name=".car.RoadPulseCarAppService"
            android:exported="true"
            tools:ignore="ExportedService">
            <!-- Android Auto must bind to this exported service; HostValidator authenticates the host. -->
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.NAVIGATION" />
            </intent-filter>
        </service>

        <service
            android:name=".alerts.CameraDataRefreshJobService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <service
            android:name=".navigation.NavInfoReceivingService"
            android:exported="false" />
    </application>

</manifest>
